public static class ArrayUtils
{
    public static int[][] ArrayFromStr(string str)
    {
        str = str.Replace(" ", string.Empty);
        var size = str.Count(x => x == '[');

        return null;
    }
}
